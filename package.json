{
  "scripts": {
    "build:tracking": "npx walkeros bundle ./walkeros.config.json --output assets/js/walkeros.bundle.js"
  },
  "devDependencies": {
    "@walkeros/cli": "^2.0.1",
    "@walkeros/web-destination-api": "^2.0.1",
    "@walkeros/web-destination-gtag": "^2.0.1",
    "@walkeros/web-source-browser": "^2.0.1"
  }
}
